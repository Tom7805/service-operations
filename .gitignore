### === Root / chung cho cả repo === ###

# Biến môi trường thật (chỉ .env.example được commit)
.env
.env.local
.env.*.local
*.env
!.env.example
!frontend/.env.example

# Log
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hệ điều hành
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE / Editor
.idea/
*.iml
*.iws
*.ipr
.vscode/*
!.vscode/extensions.json
*.swp
*.swo
*~
.classpath
.project
.settings/
.factorypath

# Nén / backup tạm
*.zip
*.tar.gz
*.rar
*.bak
*.tmp

### === Backend (Java / Maven / Spring Boot) === ###
backend/target/
backend/**/target/
backend/.mvn/wrapper/maven-wrapper.jar
backend/*.class
backend/HELP.md
backend/*.iml
backend/out/
backend/build/

# File cấu hình local override (không commit secret)
backend/src/main/resources/application-local.yml
backend/src/main/resources/application-secret.yml

### === Frontend (Node / Vite / React) === ###
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/build/
frontend/.vite/
frontend/coverage/
frontend/*.local
frontend/.eslintcache
frontend/*.tsbuildinfo

# Playwright / test artifact
frontend/playwright-report/
frontend/test-results/
frontend/e2e/playwright-report/
frontend/e2e/test-results/

### === Docker === ###
docker/**/*.env
docker-compose.override.yml
!docker-compose.override.yml.example

### === Tài liệu / dữ liệu tạo tự động === ###
docs/06-ui/screenshots/*
!docs/06-ui/screenshots/.gitkeep
docs/03-database/*.sql.bak

### === Backup CSDL / dữ liệu nhạy cảm === ###
*.sql.gz
*.dump
/backups/

### === Claude Code (công cụ/skill cá nhân, không dùng chung cả team) === ###
.claude/
# Ket qua chup man hinh / comp cua impeccable la san pham trung gian, khong commit.
# Rieng .impeccable/config.json la cau hinh CHUNG cua nhom (ignoreValues detector) nen van commit.
.impeccable/review/
.impeccable/mocks/
.impeccable/config.local.json
