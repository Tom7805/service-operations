/**
 * Bộ icon của module Tài khoản nay dùng chung với toàn dự án.
 * File này chỉ re-export để các import cũ (`../components/icons`) không phải sửa lại.
 */
export { ICONS } from '../../../components/common/icons';
