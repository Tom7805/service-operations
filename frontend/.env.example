# Sao chép thành .env rồi chỉnh nếu cần — .env KHÔNG được commit lên Git

VITE_API_BASE_URL=http://localhost:8080/api/v1

# NCL-01-CN-001-TC-03: so phut khong thao tac truoc khi tu dong dang xuat (mac dinh 30).
VITE_SESSION_IDLE_MINUTES=30
